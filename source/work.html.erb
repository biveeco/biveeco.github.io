<%= component "main_header", locals: { theme: "dark" } %>

<% component "panel", locals: {
  padding: {
    top: "wide",
    bottom: "xwide",
    x: "outside"
  },
  el: "main",
  border: "none",
  theme: "dark"
} do %>
  <h1 class="padding-bottom-wide t-font-display t-align-center">Our work</h1>
  <% component "wrapper", locals: { width: "wide" } do %>
    <%= component "projects_list", locals: { projects: data.work.projects } %>
  <% end %>
<% end %>

<% component "panel", locals: {
  el: "aside",
  theme: "dark"
} do %>
  <h2 class="t-scale-gamma t-align-center padding-bottom"><%= data.work.pro_bono.title %></h2>
  <% component "wrapper", locals: { classes: "t-content" } do %>
    <%= render_markdown(data.work.pro_bono.body) %>
  <% end %>
<% end %>
